## ADDED Requirements

### Requirement: 測試結果獨立清除控制
系統必須（MUST）在測試結果版面的標題列提供「清除紀錄」與「清除結果」的獨立控制按鈕。

#### Scenario: 點擊清除紀錄
- **WHEN** 使用者點擊標題列的「清除紀錄」按鈕
- **THEN** 系統必須清空左側的通訊回應資料，但不得影響右側的判定結果顯示。

#### Scenario: 點擊清除結果
- **WHEN** 使用者點擊標題列的「清除結果」按鈕
- **THEN** 系統必須清空右側的判定結果（NG/OK），但不得影響左側的通訊回應資料。
